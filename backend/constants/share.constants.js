export const SHARE_PERMISSION = {
  VIEW: "VIEW",
  COMMENT: "COMMENT",
  EDIT: "EDIT",
};

export const SHARE_STATUS = {
  PENDING: "PENDING",
  ACTIVE: "ACTIVE",
  REVOKED: "REVOKED",
  DECLINED: "DECLINED",
  EXPIRED: "EXPIRED",
};

export const SHARE_PERMISSION_VALUES = Object.values(SHARE_PERMISSION);
export const SHARE_STATUS_VALUES = Object.values(SHARE_STATUS);

export const SHARE_NOTIFICATION = {
  MAX_ATTEMPTS: Number(process.env.SHARE_NOTIFICATION_MAX_ATTEMPTS || 3),
  BASE_DELAY_SECONDS: Number(process.env.SHARE_NOTIFICATION_BASE_DELAY_SECONDS || 30),
  MAX_DELAY_SECONDS: Number(process.env.SHARE_NOTIFICATION_MAX_DELAY_SECONDS || 600),
};
